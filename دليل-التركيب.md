# 🏠 مذكّرة البيت — دليل التركيب الكامل

موقعكم الخاص: قائمة مشتركة بينك وبين زوجتك + إشعارات يومية على الآيفون بالاحتياجات المستحقة.
كل شيء **مجاني** ولا يحتاج بطاقة ائتمانية. الوقت المتوقع: ٣٠–٤٥ دقيقة أول مرة فقط.

**الفكرة باختصار:**
- الموقع نفسه يُستضاف مجاناً على **GitHub Pages**.
- البيانات المشتركة تُحفظ في **Firebase Firestore** (خطة Spark المجانية).
- الإشعارات اليومية يرسلها **GitHub Actions** تلقائياً كل يوم الساعة ٧ صباحاً بتوقيت الرياض.

---

## المرحلة ١: إنشاء مشروع Firebase (١٠ دقائق)

1. ادخل على **console.firebase.google.com** وسجّل بحساب Google.
2. اضغط **Create a project** (إنشاء مشروع) → سمّه مثلاً `bayt-app` → عطّل Google Analytics (ما نحتاجه) → **Create**.
3. من القائمة اليسرى: **Build → Firestore Database → Create database**:
   - اختر Location: `eur3` أو أي موقع قريب → التالي.
   - اختر **Start in production mode** → **Enable**.
4. بعد ما تفتح قاعدة البيانات، روح لتبويب **Rules** واحذف الموجود والصق محتوى ملف `firestore.rules` (المرفق في هذه الحزمة) → **Publish**.
5. ارجع للصفحة الرئيسية للمشروع → اضغط أيقونة **`</>`** (Web) لإضافة تطبيق ويب:
   - سمّه `bayt` → **Register app** (لا تفعّل Hosting).
   - راح يظهر لك كود فيه `const firebaseConfig = { apiKey: "...", ... }`.
   - **انسخ القيم** وافتح ملف `index.html` بأي محرر نصوص، وفي أعلى الملف بتلقى قسم واضح مكتوب فيه «إعدادات Firebase — الصق إعداداتك هنا» — استبدل كل `الصق_هنا` بالقيم اللي نسختها. احفظ الملف.

## المرحلة ٢: مفتاح الحساب الخدمي (للإشعارات) (٣ دقائق)

1. في Firebase: اضغط ⚙ (أعلى اليسار) → **Project settings** → تبويب **Service accounts**.
2. اضغط **Generate new private key** → **Generate key** → راح ينزل ملف JSON.
3. افتح الملف بمحرر نصوص وخلّه جاهز — بنحتاج محتواه في المرحلة ٤. ⚠️ هذا الملف سرّي: لا ترسله لأحد ولا ترفعه داخل ملفات الموقع.

## المرحلة ٣: رفع الموقع على GitHub (١٠ دقائق)

1. سوّ حساب في **github.com** (مجاني).
2. اضغط **+** (أعلى اليمين) → **New repository**:
   - الاسم: `bayt` (أو أي اسم).
   - النوع: **Public** (لازم يكون عام عشان الاستضافة المجانية — لا تقلق: البيانات مو هنا، هي في Firebase، ورمز بيتكم ما ينحفظ في هذه الملفات).
   - **Create repository**.
3. في صفحة المستودع الجديد اضغط **uploading an existing file** → اسحب **كل ملفات هذه الحزمة** (بما فيها مجلد `notify` ومجلد `.github` — مهم جداً!) → **Commit changes**.
   - ملاحظة: بعض المتصفحات ما ترفع المجلدات بالسحب؛ الحل: ارفع الملفات الرئيسية أولاً، ثم اضغط **Add file → Create new file** واكتب في اسم الملف `notify/package.json` والصق محتواه، وكرر لـ `notify/send.js` و `.github/workflows/daily-notify.yml`.
4. فعّل الاستضافة: **Settings → Pages** → تحت Source اختر **Deploy from a branch** → Branch: `main` و `/ (root)` → **Save**.
5. انتظر دقيقتين، وبيظهر رابط موقعكم بالشكل:
   `https://اسم-حسابك.github.io/bayt/`
   هذا هو رابط موقعكم الدائم! 🎉

## المرحلة ٤: أسرار الإشعارات (٥ دقائق)

في مستودعك على GitHub: **Settings → Secrets and variables → Actions → New repository secret**، وأضف ثلاثة أسرار (الاسم بالضبط كما هو):

| الاسم | القيمة |
|---|---|
| `FIREBASE_SERVICE_ACCOUNT` | الصق **كامل محتوى ملف JSON** من المرحلة ٢ |
| `VAPID_PUBLIC_KEY` | `BB5cxv-GNGc8S_hnO-HUabCJlIkzC6DHElKIJM8fEpemRjSZY5C8v7f2UBZSe5FBCxk05_zbogOS0xJaPOBIY-s` |
| `VAPID_PRIVATE_KEY` | `4ukP-ZGpqBLcBYJtqdfCNxRbatMHUP1z1M1OD7lC9yc` |

> 💡 هذه مفاتيح توليد عشوائي خاصة بمشروعكم. إذا حبيت تولّد مفاتيح جديدة كلياً لاحقاً، تقدر من موقع vapidkeys.com — بس لا تنسى تحدث المفتاح العام في `index.html` أيضاً.

## المرحلة ٥: الإعداد على الآيفون (أنت وزوجتك) (٥ دقائق لكل جوال)

هذي الخطوات **إلزامية على الآيفون** — آبل تشترط إضافة الموقع للشاشة الرئيسية قبل السماح بالإشعارات (iOS 16.4 وأحدث):

1. افتح رابط موقعكم في **سفاري**.
2. اضغط زر **المشاركة** (المربع مع السهم ⬆) → **إضافة إلى الشاشة الرئيسية** → **إضافة**.
3. **أغلق سفاري وافتح التطبيق من الأيقونة الجديدة** على الشاشة الرئيسية.
4. أول شخص (أنت): اضغط **«إنشاء بيت جديد»** → افتح ⚙ → انسخ رمز البيت وأرسله لزوجتك (واتساب مثلاً).
5. زوجتك: تسوي نفس خطوات ١–٣، ثم تختار **«الانضمام لبيت موجود»** وتلصق الرمز.
6. كل واحد منكم يضغط زر **«فعّل الإشعارات»** الأصفر في أعلى الصفحة → **السماح**.

خلاص! 🎉 كل يوم الساعة ٧ صباحاً، إذا فيه احتياجات حان موعدها، يوصلكم إشعار على الجوالين.

## تجربة فورية للإشعارات (اختياري)

ما تبي تنتظر ليوم بكرة؟ في GitHub: تبويب **Actions** → اختر **تذكير-يومي** من اليسار → زر **Run workflow** → **Run workflow**. خلال دقيقة يوصل الإشعار (بشرط وجود شيء «مستحق» في قائمتكم — أي عنصر جديد ما ضغطتوا له «تم» يُعتبر مستحقاً).

## تغيير وقت الإشعار اليومي

افتح ملف `.github/workflows/daily-notify.yml` وعدّل السطر `cron: '0 4 * * *'`:
- الرقم الثاني هو الساعة بتوقيت UTC (الرياض = UTC+3).
- أمثلة: `0 4` = ٧ صباحاً، `0 15` = ٦ مساءً، `30 16` = ٧:٣٠ مساءً.

## حل المشاكل الشائعة

- **«تعذّر الاتصال» في التطبيق** → تأكد أنك لصقت إعدادات Firebase صح في `index.html`، وأن قواعد Firestore منشورة (المرحلة ١ خطوة ٤).
- **زر تفعيل الإشعارات ما يشتغل** → لازم تفتح التطبيق **من أيقونة الشاشة الرئيسية**، مو من سفاري مباشرة. وتأكد أن نظام الجوال iOS 16.4 أو أحدث.
- **الإشعار ما وصل** → افتح GitHub → Actions → آخر تشغيل، وشوف السجل: إذا مكتوب `sent ✓` فالإرسال تم؛ تأكد أن الإشعارات مسموحة للتطبيق في إعدادات الآيفون → الإشعارات.
- **حدّثت `index.html`** → ارفع الملف الجديد لنفس المستودع (Add file → Upload) وGitHub Pages يتحدث تلقائياً خلال دقائق.

## ملاحظة أمان صغيرة

الوصول لقائمتكم محمي برمز البيت (٢٤ حرفاً عشوائياً يستحيل تخمينه عملياً). لا تشاركوا الرمز مع غير أهل البيت. البيانات المخزنة هي أسماء أغراض وتواريخ فقط — لا معلومات حساسة.
